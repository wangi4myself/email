# Actix-web、Tokio 与 Reqwest 介绍

本文档介绍 Rust 异步生态中三个核心组件，并结合本项目的实际代码进行说明。

## 快速类比：Node.js 开发者视角

| Rust 生态     | Node.js 生态             | 作用                               |
| ------------- | ------------------------ | ---------------------------------- |
| **Tokio**     | Node.js 事件循环 (libuv) | 异步运行时，处理 I/O 事件          |
| **actix-web** | Express.js / Fastify     | Web 框架，处理 HTTP 请求（服务端） |
| **reqwest**   | axios / node-fetch       | HTTP 客户端，发送请求（客户端）    |
| `async/await` | `async/await`            | 语法糖，编写异步代码               |

## Tokio：异步运行时

### 是什么？

Tokio 是 Rust 的**异步运行时**（Async Runtime）。

在 Node.js 中，V8 引擎自带事件循环（libuv），你不需要关心它。但 Rust 标准库**不包含异步运行时**，需要自己选择一个。Tokio 是最流行的选择。

### 核心概念

```
┌─────────────────────────────────────────────┐
│              Tokio Runtime                   │
│  ┌─────────────────────────────────────┐    │
│  │         Event Loop (事件循环)        │    │
│  │  - 管理所有异步任务                  │    │
│  │  - 调度 Future 执行                  │    │
│  │  - 处理 I/O 事件                     │    │
│  └─────────────────────────────────────┘    │
│  ┌─────────────────────────────────────┐    │
│  │       Thread Pool (线程池)           │    │
│  │  - 多线程执行任务                    │    │
│  │  - 充分利用多核 CPU                  │    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

### 在项目中的使用

```rust
// src/main.rs
#[tokio::main]  // 👈 这个宏初始化 Tokio 运行时
async fn main() -> std::io::Result<()> {
    // ...
    run(listener, connection_pool, email_client)?.await?;  // 👈 .await 需要运行时来执行
    Ok(())
}
```

**`#[tokio::main]` 宏展开后相当于：**

```rust
fn main() -> std::io::Result<()> {
    tokio::runtime::Builder::new_multi_thread()
        .enable_all()
        .build()
        .unwrap()
        .block_on(async {
            // 你的 async main 代码
        })
}
```

### Cargo.toml 配置

```toml
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
```

| Feature           | 说明                         |
| ----------------- | ---------------------------- |
| `macros`          | 启用 `#[tokio::main]` 等宏   |
| `rt-multi-thread` | 多线程运行时（生产环境推荐） |

### 与 Node.js 的关键区别

| 特性         | Node.js        | Tokio            |
| ------------ | -------------- | ---------------- |
| 线程模型     | 单线程事件循环 | 多线程运行时     |
| CPU 密集任务 | 阻塞主线程     | 可分配到其他线程 |
| 默认可用     | ✅ 内置        | ❌ 需要引入      |

## Actix-web：Web 框架

### 是什么？

Actix-web 是基于 Actix actor 框架构建的高性能 Web 框架，类似于 Node.js 中的 Express 或 Fastify。

### 性能对比

根据 TechEmpower 基准测试，Actix-web 是最快的 Web 框架之一：

```
Actix-web (Rust)    ████████████████████████████ 600k+ req/s
Fastify (Node.js)   ████████████                  280k req/s
Express (Node.js)   ████                          100k req/s
```

### 核心概念

```
┌───────────────────────────────────────────────────────┐
│                    HttpServer                          │
│  ┌─────────────────────────────────────────────────┐  │
│  │                    App                           │  │
│  │  ┌─────────────────────────────────────────┐    │  │
│  │  │              Middleware                  │    │  │
│  │  │  (TracingLogger, CORS, Auth, etc.)      │    │  │
│  │  └─────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────┐    │  │
│  │  │               Routes                     │    │  │
│  │  │  /health_check  →  health_check()       │    │  │
│  │  │  /subscriptions →  subscribe()          │    │  │
│  │  └─────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────┐    │  │
│  │  │            App Data (共享状态)           │    │  │
│  │  │  - db_pool (数据库连接池)               │    │  │
│  │  │  - email_client (邮件客户端)            │    │  │
│  │  └─────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────┘
```

### 在项目中的使用

```rust
// src/startup.rs
use actix_web::{dev::Server, web, App, HttpServer};

pub fn run(
    listener: TcpListener,
    db_pool: PgPool,
    email_client: EmailClient,
) -> Result<Server, std::io::Error> {
    // 1. 包装共享状态
    let db_pool = web::Data::new(db_pool);        // 类似 Express 的 app.locals
    let email_client = web::Data::new(email_client);

    // 2. 创建 HTTP 服务器
    let server = HttpServer::new(move || {
        App::new()
            // 3. 添加中间件 (类似 Express 的 app.use())
            .wrap(TracingLogger::default())
            // 4. 定义路由 (类似 Express 的 app.get() / app.post())
            .route("/health_check", web::get().to(health_check))
            .route("/subscriptions", web::post().to(subscribe))
            // 5. 注入共享状态
            .app_data(db_pool.clone())
            .app_data(email_client.clone())
    })
    .listen(listener)?
    .run();

    Ok(server)
}
```

### 对比 Express.js

```javascript
// Express.js 版本
const express = require("express");
const app = express();

// 中间件
app.use(loggingMiddleware);

// 共享状态
app.locals.dbPool = dbPool;
app.locals.emailClient = emailClient;

// 路由
app.get("/health_check", healthCheck);
app.post("/subscriptions", subscribe);

app.listen(8000);
```

```rust
// Actix-web 版本
let server = HttpServer::new(move || {
    App::new()
        // 中间件
        .wrap(TracingLogger::default())
        // 共享状态
        .app_data(web::Data::new(db_pool.clone()))
        .app_data(web::Data::new(email_client.clone()))
        // 路由
        .route("/health_check", web::get().to(health_check))
        .route("/subscriptions", web::post().to(subscribe))
})
.bind("127.0.0.1:8000")?
.run();
```

## 两者如何协作

```
请求流程:

  HTTP Request
       │
       ▼
┌─────────────────┐
│   Actix-web     │  接收请求，分发到 handler
│   (Web 框架)    │
└───────┬─────────┘
        │
        ▼
┌─────────────────┐
│  async handler  │  执行业务逻辑 (async fn)
│  subscribe()    │
└───────┬─────────┘
        │ .await
        ▼
┌─────────────────┐
│     Tokio       │  执行数据库查询、网络请求等 I/O
│   (异步运行时)   │  在等待 I/O 时处理其他请求
└───────┬─────────┘
        │
        ▼
  HTTP Response
```

### 实际代码流程

```rust
// 1. Tokio 运行时启动
#[tokio::main]
async fn main() {
    // 2. 创建 Actix-web 服务器
    let server = run(listener, db_pool, email_client)?;

    // 3. Tokio 运行时驱动服务器运行
    server.await?;
}

// 4. 当请求到达，Actix 调用 handler
async fn subscribe(
    form: web::Form<FormData>,
    pool: web::Data<PgPool>,
) -> HttpResponse {
    // 5. Tokio 执行数据库查询
    sqlx::query!(...)
        .execute(pool.get_ref())
        .await  // 👈 在等待时，Tokio 可以处理其他请求
        .expect("Failed to execute query.");

    HttpResponse::Ok().finish()
}
```

## 为什么需要两者？

| 问题                           | Tokio 解决     | Actix-web 解决 | Reqwest 解决 |
| ------------------------------ | -------------- | -------------- | ------------ |
| 如何运行异步代码？             | ✅             |                |              |
| 如何处理 HTTP 请求（服务端）？ |                | ✅             |              |
| 如何发送 HTTP 请求（客户端）？ |                |                | ✅           |
| 如何实现路由？                 |                | ✅             |              |
| 如何注入依赖？                 |                | ✅             |              |
| 如何并发处理请求？             | ✅             |                |              |
| 如何执行数据库查询？           | ✅ (通过 sqlx) |                |              |
| 如何调用外部 API？             |                |                | ✅           |

**简单总结：**

- **Tokio** = 引擎（提供动力）
- **Actix-web** = 车身（接收请求）
- **Reqwest** = 信使（发送请求）

没有 Tokio，异步代码无法运行。没有 Actix-web，你需要自己解析 HTTP 请求。

## Reqwest：HTTP 客户端

### 是什么？

Reqwest 是 Rust 最流行的 **HTTP 客户端**库，类似于 Node.js 中的 axios 或 node-fetch。

**关键区别：**

- **Actix-web** = HTTP 服务端（接收请求）
- **Reqwest** = HTTP 客户端（发送请求）

### 对比 Node.js

| Rust (reqwest)                 | Node.js                 | 说明                   |
| ------------------------------ | ----------------------- | ---------------------- |
| `reqwest::Client`              | `axios.create()`        | 创建可复用的客户端实例 |
| `client.get(url)`              | `axios.get(url)`        | 发送 GET 请求          |
| `client.post(url).json(&data)` | `axios.post(url, data)` | 发送 POST JSON 请求    |
| `.send().await`                | `await axios.get()`     | 异步等待响应           |

### 在项目中的使用

```rust
// src/email_client.rs
use reqwest::Client;

pub struct EmailClient {
    http_client: Client,      // 👈 reqwest 客户端
    base_url: String,
    sender: SubscriberEmail,
}

impl EmailClient {
    pub fn new(base_url: String, sender: SubscriberEmail) -> Self {
        Self {
            http_client: Client::new(),  // 👈 创建客户端实例
            base_url,
            sender,
        }
    }

    pub async fn send_email(&self, ...) -> Result<(), reqwest::Error> {
        self.http_client
            .post(&format!("{}/email", self.base_url))
            .json(&email_body)
            .send()        // 👈 发送请求
            .await?;       // 👈 异步等待
        Ok(())
    }
}
```

### 对比 axios

```javascript
// Node.js (axios) 版本
const axios = require("axios");

class EmailClient {
  constructor(baseUrl, sender) {
    this.httpClient = axios.create({ baseURL: baseUrl });
    this.sender = sender;
  }

  async sendEmail(recipient, subject, htmlContent, textContent) {
    await this.httpClient.post("/email", {
      from: this.sender,
      to: recipient,
      subject,
      html: htmlContent,
      text: textContent,
    });
  }
}
```

```rust
// Rust (reqwest) 版本
use reqwest::Client;

pub struct EmailClient {
    http_client: Client,
    base_url: String,
    sender: SubscriberEmail,
}

impl EmailClient {
    pub async fn send_email(
        &self,
        recipient: &SubscriberEmail,
        subject: &str,
        html_content: &str,
        text_content: &str,
    ) -> Result<(), reqwest::Error> {
        self.http_client
            .post(&format!("{}/email", self.base_url))
            .json(&serde_json::json!({
                "from": self.sender.as_ref(),
                "to": recipient.as_ref(),
                "subject": subject,
                "html": html_content,
                "text": text_content,
            }))
            .send()
            .await?
            .error_for_status()?;  // 类似 axios 的自动错误处理
        Ok(())
    }
}
```

### Cargo.toml 配置

```toml
reqwest = { version = "0.11", default-features = false, features = [
    "json",          # 支持 JSON 序列化/反序列化
    "rustls-tls",    # 使用 rustls 作为 TLS 后端（纯 Rust 实现）
    "cookies",       # 支持 Cookie 处理
]}
```

| Feature                    | 说明                                               |
| -------------------------- | -------------------------------------------------- |
| `json`                     | 启用 `.json()` 方法                                |
| `rustls-tls`               | 使用 Rust 原生 TLS（vs `native-tls` 使用系统 TLS） |
| `cookies`                  | 自动处理 Cookie                                    |
| `default-features = false` | 禁用默认的 `native-tls`，减少依赖                  |

### 常用操作示例

```rust
use reqwest::Client;

// 创建客户端（建议复用，不要每次请求都创建）
let client = Client::new();

// GET 请求
let response = client
    .get("https://api.example.com/users")
    .header("Authorization", "Bearer token")
    .send()
    .await?;

// POST JSON
let user = serde_json::json!({ "name": "Alice", "email": "alice@example.com" });
let response = client
    .post("https://api.example.com/users")
    .json(&user)
    .send()
    .await?;

// 解析 JSON 响应
let user: User = response.json().await?;

// 获取状态码
if response.status().is_success() {
    println!("Success!");
}

// 获取响应文本
let text = response.text().await?;
```

### 与 Tokio 的关系

Reqwest 依赖 Tokio 运行时来执行异步 HTTP 请求：

```
发送邮件流程:

  EmailClient.send_email()
       │
       ▼
┌─────────────────┐
│    Reqwest      │  构建 HTTP 请求
│  (HTTP 客户端)   │
└───────┬─────────┘
        │ .send().await
        ▼
┌─────────────────┐
│     Tokio       │  执行网络 I/O
│   (异步运行时)   │  处理 TCP 连接、TLS 握手等
└───────┬─────────┘
        │
        ▼
  外部邮件服务 API
```

### 为什么复用 Client？

```rust
// ❌ 不推荐：每次请求创建新客户端
async fn send_email() {
    let client = Client::new();  // 每次都创建新的连接池
    client.post(...).send().await;
}

// ✅ 推荐：复用客户端实例
struct EmailClient {
    http_client: Client,  // 复用连接池
}
```

类似于 Node.js 中复用 axios 实例：

- 连接池复用，减少 TCP 握手开销
- 更好的性能
- 更少的资源消耗

## 三者协作关系

```
完整请求流程（用户订阅 → 发送确认邮件）:

  用户 HTTP 请求 (POST /subscriptions)
       │
       ▼
┌─────────────────┐
│   Actix-web     │  接收请求，路由到 handler
│  (Web 服务端)    │
└───────┬─────────┘
        │
        ▼
┌─────────────────┐
│  subscribe()    │  业务逻辑：保存用户，发送邮件
│   handler       │
└───────┬─────────┘
        │
        ├──────────────────────┐
        │                      │
        ▼                      ▼
┌─────────────────┐    ┌─────────────────┐
│     SQLx        │    │    Reqwest      │
│  (数据库查询)    │    │  (HTTP 客户端)   │
└───────┬─────────┘    └───────┬─────────┘
        │                      │
        └──────────┬───────────┘
                   │ .await
                   ▼
           ┌─────────────────┐
           │     Tokio       │  执行所有异步 I/O
           │   (异步运行时)   │
           └───────┬─────────┘
                   │
        ┌──────────┴───────────┐
        ▼                      ▼
   PostgreSQL            邮件服务 API
```

## 其他常见选择

### 异步运行时

| 运行时    | 特点               |
| --------- | ------------------ |
| **Tokio** | 最流行，功能最全面 |
| async-std | 类似标准库 API     |
| smol      | 轻量级             |

### Web 框架

| 框架          | 特点                         |
| ------------- | ---------------------------- |
| **Actix-web** | 最快，功能丰富               |
| Axum          | Tokio 官方团队出品，更现代   |
| Rocket        | 开发体验好，像 Ruby on Rails |
| Warp          | 基于 Filter 组合             |

## 总结

对于熟悉 Node.js 的开发者：

```
Node.js 架构:
┌─────────────────────────────────────┐
│  Express.js (Web 框架)              │
│  ┌───────────────────────────────┐  │
│  │  V8 + libuv (内置运行时)       │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘

Rust 架构:
┌─────────────────────────────────────┐
│  Actix-web (Web 框架)               │
│  ┌───────────────────────────────┐  │
│  │  Tokio (需要显式引入的运行时)  │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

核心差异：Node.js 把运行时内置了，Rust 需要你自己选择和引入。这提供了更大的灵活性，但也需要更多的理解。
