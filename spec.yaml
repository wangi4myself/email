name: email-subscription-service
region: sgp
services:
  - name: email-subscription-service
    dockerfile_path: Dockerfile
    source_dir: .
    github:
      repo: YOUR_USERNAME/email # 需替换
      branch: main
      deploy_on_push: true
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
